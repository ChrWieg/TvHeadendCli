<mah:MetroWindow x:Class="TvHeadendGui.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        Title="{Binding Title}" MaxWidth="600" MaxHeight="500"
        xmlns:interactivity="http://schemas.microsoft.com/expression/2010/interactivity"
        SizeToContent="Width"
        WindowStartupLocation="CenterScreen"
         MinHeight="400"
         MinWidth="700"
                 >

    <interactivity:Interaction.Triggers>
        <interactivity:EventTrigger EventName="ContentRendered">
            <interactivity:EventTrigger.Actions>
                <prism:InvokeCommandAction Command="{Binding ContentRendered}"></prism:InvokeCommandAction>
            </interactivity:EventTrigger.Actions>
        </interactivity:EventTrigger>
    </interactivity:Interaction.Triggers>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Column="0" prism:RegionManager.RegionName="NavBarRegion" Margin="10"/>
        <ContentControl Grid.Column="1" prism:RegionManager.RegionName="ContentRegion" Margin="10" />
    </Grid>
</mah:MetroWindow>
